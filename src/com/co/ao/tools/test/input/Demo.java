package com.co.ao.tools.test.input;

import com.co.ao.tools.array.out.Impresion;

public class Demo {
    public static void main(String[] args) {
        // Queueexample queue = new Queueexample();
        // System.out.println(queue.getCharactersQueue().size());
        ArrayExample array = new ArrayExample();

        Impresion.porPantalla().de(array.getIdentificationExampleArray());
    }
}
